# Copy this file to .env and set your signing identity for a stable signature
# (so macOS keeps the Accessibility grant across rebuilds). .env is gitignored.
# List your identities: security find-identity -v -p codesigning
CODESIGN_IDENTITY="Apple Development: Your Name (TEAMID)"
